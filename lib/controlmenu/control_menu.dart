import 'package:get/get.dart';

class controlmenu extends GetxController{

  var item = 0.obs;
  cambiar (posicion) {
    item (posicion);

  }     //=> indice = posicion.to;

  obtenerIndice(){


  }
}
